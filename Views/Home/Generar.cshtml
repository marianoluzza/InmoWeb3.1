@model IEnumerable<Type>
@{
	ViewData["Title"] = "Generar";
	InmoWeb3._1.Extra.MySqlGen g = new InmoWeb3._1.Extra.MySqlGen();
}

<h1>Generar</h1>

@foreach (var item in Model)
{
	<h2>@item.Name</h2>
	<pre>
	@g.Create(item)
	</pre>
}